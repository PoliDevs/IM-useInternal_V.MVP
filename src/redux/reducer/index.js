
const initalState = {};

export const  rootReducer = (state= initalState, action) => {
  switch (action.type) {
    case value:
      
      break;
  
    default:
      return state;
  }
}

export default rootReducer;